import { ShopDetailPipe } from './shop-detail.pipe';

describe('ShopDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
